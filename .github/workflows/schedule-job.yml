name: Rust

on:
  schedule:
    - cron: "0 0-23 * * *"
env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run
        run: cargo run
